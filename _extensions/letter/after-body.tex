$if(closing)$
\closing{$closing$\\[0.2cm]\hspace*{0.5cm}
$if(signature)$
\includegraphics[height=1.5cm]{$signature$}
$endif$
}
$endif$

$if(ps)$
\vspace{\baselineskip} % Add a line of space before the PS
\ps{$ps$}             % Place the PS here, BEFORE \vfill
$endif$

\vfill                  % \vfill now pushes encl/cc down

$if(encl)$
\encl{$for(encl)$$encl$$sep$\\$endfor$} % encl stays at the bottom
$endif$
$if(cc)$
\cc{$for(cc)$$cc$$sep$\\$endfor$}     % cc stays at the bottom
$endif$

\end{letter}
